name: Build hashcat

env:
  INCLUDE_PATHS: |
    charsets/
    docs/
    extra/
    layouts/
    masks/
    modules/
    OpenCL/
    rules/
    bridges/
    Python/
    tools/*.py
    tools/*.pl
    tunings/
    example*
    hashcat*

on:
  workflow_dispatch:
    inputs:
      force:
        description: "强制编译 (1=强制, 0=仅检测更新)"
        required: false
        default: "0"
  schedule:
    - cron: "0 0 * * *"
  push:
    branches:
      - "*"
    paths:
      - '.github/workflows/build.yml'

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    env:
      FORCE_BUILD: ${{ github.event.inputs.force || '0' }}

    steps:
      # 关键修改：使用固定的缓存key，而不是动态的
      - name: Restore last upstream SHA cache
        uses: actions/cache@v5
        with:
          path: .cache
          key: upstream-sha-cache  # 改为固定key
          restore-keys: |
            upstream-sha-cache

      # 检查上游是否有更新
      - id: check_upstream
        name: Check upstream changes
        shell: bash
        run: |
          mkdir -p .cache

          # 获取上游最新 commit
          git clone --depth=1 https://github.com/hashcat/hashcat.git tmprepo
          NEW_SHA=$(cd tmprepo && git rev-parse HEAD)
          echo "new_sha=$NEW_SHA" >> $GITHUB_OUTPUT
          echo "Found upstream NEW_SHA=$NEW_SHA"

          # 读取缓存的SHA
          if [ -f .cache/last_sha ]; then
            OLD_SHA=$(cat .cache/last_sha)
            echo "Found cached OLD_SHA=$OLD_SHA"
          else
            OLD_SHA=""
            echo "No cached SHA found"
          fi
          echo "old_sha=$OLD_SHA" >> $GITHUB_OUTPUT

          # 判断是否需要构建
          if [ "${FORCE_BUILD}" = "1" ]; then
            echo "FORCE_BUILD=1 -> 强制编译"
            echo "skip=false" >> $GITHUB_OUTPUT
          else
            if [ "$NEW_SHA" = "$OLD_SHA" ]; then
              echo "No upstream changes (current: $NEW_SHA), skipping build."
              echo "skip=true" >> $GITHUB_OUTPUT
            else
              echo "Detected upstream changes: $OLD_SHA -> $NEW_SHA"
              echo "skip=false" >> $GITHUB_OUTPUT
            fi
          fi
          
          # 清理临时仓库
          rm -rf tmprepo

      # 保存SHA到缓存（无论是否构建，都要保存当前SHA）
      - name: Save current SHA to cache
        if: always()  # 总是执行，确保SHA被保存
        run: |
          mkdir -p .cache
          echo "${{ steps.check_upstream.outputs.new_sha }}" > .cache/last_sha
          echo "Saved SHA ${{ steps.check_upstream.outputs.new_sha }} to .cache/last_sha"

      # 下面的所有构建相关步骤都加上条件
      - name: Checkout repository
        if: ${{ steps.check_upstream.outputs.skip != 'true' }}
        uses: actions/checkout@v4

      - name: Set up Rust
        if: ${{ steps.check_upstream.outputs.skip != 'true' }}
        uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          override: true
          profile: minimal

      - name: Add Windows Rust target
        if: ${{ steps.check_upstream.outputs.skip != 'true' }}
        run: rustup target add x86_64-pc-windows-gnu

      # Cargo 缓存
      - name: Cache cargo registry + index
        if: ${{ steps.check_upstream.outputs.skip != 'true' }}
        uses: actions/cache@v5
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ${{ runner.os }}-cargo-registry-${{ hashFiles('hashcat/Rust/generic_hash/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-registry-

      - name: Cache cargo build
        if: ${{ steps.check_upstream.outputs.skip != 'true' }}
        uses: actions/cache@v5
        with:
          path: hashcat/Rust/generic_hash/target
          key: ${{ runner.os }}-cargo-build-${{ hashFiles('hashcat/Rust/generic_hash/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-build-

      - name: Install Build Dependencies
        if: ${{ steps.check_upstream.outputs.skip != 'true' }}
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            build-essential \
            gcc-mingw-w64-x86-64 \
            g++-mingw-w64-x86-64 \
            make git zstd python3-dev cmake p7zip-full

      - name: Clone Source Code
        if: ${{ steps.check_upstream.outputs.skip != 'true' }}
        run: |
          rm -rf hashcat win-iconv
          git clone https://github.com/hashcat/hashcat.git
          git clone https://github.com/win-iconv/win-iconv.git

      - name: Compile and Install win-iconv
        if: ${{ steps.check_upstream.outputs.skip != 'true' }}
        run: |
          cd win-iconv/
          cmake \
            -D WIN_ICONV_BUILD_EXECUTABLE=OFF \
            -D CMAKE_INSTALL_PREFIX=/opt/win-iconv-64 \
            -D CMAKE_CXX_COMPILER=$(which x86_64-w64-mingw32-g++) \
            -D CMAKE_C_COMPILER=$(which x86_64-w64-mingw32-gcc) \
            -D CMAKE_SYSTEM_NAME=Windows
          sudo make install
          cd ../

      - name: Install Windows Python
        if: ${{ steps.check_upstream.outputs.skip != 'true' }}
        run: |
          wget https://repo.msys2.org/mingw/mingw64/mingw-w64-x86_64-python-3.12.10-1-any.pkg.tar.zst
          sudo mkdir -p /opt/win-python
          sudo tar --zstd -xf mingw-w64-x86_64-python-3.12.10-1-any.pkg.tar.zst -C /opt/win-python

      - name: Build hashcat
        if: ${{ steps.check_upstream.outputs.skip != 'true' }}
        run: |
          cd hashcat/
          make -j$(nproc) win V=1

      - name: Get Hashcat Version String
        if: ${{ steps.check_upstream.outputs.skip != 'true' }}
        id: get_version
        run: |
          cd hashcat/
          VERSION_STRING=$(git describe --tags --always)
          echo "version=${VERSION_STRING}" >> $GITHUB_OUTPUT

      - name: Package Release Artifact
        if: ${{ steps.check_upstream.outputs.skip != 'true' }}
        id: package_release
        run: |
          cd hashcat/
          ARCHIVE_NAME="hashcat_${{ steps.get_version.outputs.version }}.7z"
          7z a ../$ARCHIVE_NAME $(echo "${{ env.INCLUDE_PATHS }}" | tr '\n' ' ')
          echo "archive_path=./$ARCHIVE_NAME" >> $GITHUB_OUTPUT

      - name: Create Release
        if: ${{ steps.check_upstream.outputs.skip != 'true' }}
        uses: ncipollo/release-action@v1
        with:
          artifacts: ${{ steps.package_release.outputs.archive_path }}
          name: hashcat ${{ steps.get_version.outputs.version }}
          tag: ${{ steps.get_version.outputs.version }}
          allowUpdates: true
          token: ${{ secrets.GITHUB_TOKEN }}
